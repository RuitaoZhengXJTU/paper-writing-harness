# Scratch workspace

`previews/` contains unapproved structural designs. A preview becomes eligible for manuscript implementation only after explicit approval by preview ID. `analysis/` and `rejected-drafts/` never count as approved manuscript content.
